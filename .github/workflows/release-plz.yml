name: Release-plz

on:
  push:
    branches: [master]
  workflow_dispatch:
    inputs:
      dry_run:
        description: Run release-plz release in dry-run mode and skip release PR/publish.
        type: boolean
        default: true
      build_wheels:
        description: Build the wheel matrix even if no release was created.
        type: boolean
        default: false
      publish_pypi:
        description: Publish built artifacts to PyPI for guarded manual recovery.
        type: boolean
        default: false
      expected_version:
        description: Expected package version for guarded manual PyPI publish.
        type: string
        default: ""

env:
  CARGO_TERM_COLOR: always
  MATURIN_VERSION: "1.7"

jobs:
  release-plz-release:
    name: Release-plz release
    runs-on: ubuntu-latest
    if: ${{ github.repository_owner == 'sase-org' }}
    permissions:
      contents: write
      pull-requests: read
    outputs:
      releases_created: ${{ steps.release-plz.outputs.releases_created }}
      releases: ${{ steps.release-plz.outputs.releases }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: release-plz
          workspaces: ". -> target"

      - name: Run release-plz
        id: release-plz
        uses: release-plz/action@v0.5
        with:
          command: release
          dry_run: ${{ github.event_name == 'workflow_dispatch' && inputs.dry_run || '' }}
        env:
          GITHUB_TOKEN: ${{ secrets.SASE_RELEASE_TOKEN || secrets.GITHUB_TOKEN }}

      - name: Print release-plz release output
        env:
          RELEASES: ${{ steps.release-plz.outputs.releases }}
          RELEASES_CREATED: ${{ steps.release-plz.outputs.releases_created }}
        run: |
          echo "releases_created: ${RELEASES_CREATED}"
          echo "releases: ${RELEASES}"

  release-plz-pr:
    name: Release-plz PR
    needs: release-plz-release
    runs-on: ubuntu-latest
    if: ${{ github.repository_owner == 'sase-org' && !(github.event_name == 'workflow_dispatch' && inputs.dry_run) }}
    permissions:
      contents: write
      pull-requests: write
    concurrency:
      group: release-plz-${{ github.ref }}
      cancel-in-progress: false
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: release-plz
          workspaces: ". -> target"

      - name: Run release-plz
        id: release-plz
        uses: release-plz/action@v0.5
        with:
          command: release-pr
        env:
          GITHUB_TOKEN: ${{ secrets.SASE_RELEASE_TOKEN || secrets.GITHUB_TOKEN }}

      - name: Print release-plz PR output
        env:
          PR: ${{ steps.release-plz.outputs.pr }}
          PRS_CREATED: ${{ steps.release-plz.outputs.prs_created }}
        run: |
          echo "prs_created: ${PRS_CREATED}"
          echo "pr: ${PR}"

  linux:
    name: linux ${{ matrix.target }}
    needs: release-plz-release
    runs-on: ubuntu-latest
    if: ${{ github.repository_owner == 'sase-org' && ((needs.release-plz-release.outputs.releases_created == 'true' && !(github.event_name == 'workflow_dispatch' && inputs.dry_run)) || (github.event_name == 'workflow_dispatch' && inputs.build_wheels)) }}
    strategy:
      fail-fast: false
      matrix:
        target: [x86_64, aarch64]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: release-linux-${{ matrix.target }}
          workspaces: ". -> target"

      - name: Build wheel
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          working-directory: crates/sase_core_py
          args: --release --out dist --strip
          manylinux: "2_28"

      - name: Smoke test (x86_64 only)
        if: matrix.target == 'x86_64'
        run: |
          python -m venv /tmp/smoke
          /tmp/smoke/bin/pip install --upgrade pip
          /tmp/smoke/bin/pip install crates/sase_core_py/dist/*.whl
          /tmp/smoke/bin/python -c "import sase_core_rs; print('import ok')"
          /tmp/smoke/bin/python -c "import sase_core_rs; print('parse_query ok:', sase_core_rs.parse_query('status:Ready'))"
          /tmp/smoke/bin/python - <<'PY'
          import sase_core_rs
          try:
              sase_core_rs.parse_query('(')
          except ValueError as e:
              print('expected ValueError on bad query:', e)
          else:
              raise SystemExit('parse_query("(") should have raised ValueError')
          PY

      - uses: actions/upload-artifact@v4
        with:
          name: wheels-linux-${{ matrix.target }}
          path: crates/sase_core_py/dist

  macos:
    name: macos universal2
    needs: release-plz-release
    runs-on: macos-latest
    if: ${{ github.repository_owner == 'sase-org' && ((needs.release-plz-release.outputs.releases_created == 'true' && !(github.event_name == 'workflow_dispatch' && inputs.dry_run)) || (github.event_name == 'workflow_dispatch' && inputs.build_wheels)) }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: release-macos-universal2
          workspaces: ". -> target"

      - name: Build wheel
        uses: PyO3/maturin-action@v1
        with:
          target: universal2-apple-darwin
          working-directory: crates/sase_core_py
          args: --release --out dist --strip

      - name: Smoke test
        run: |
          python -m venv /tmp/smoke
          /tmp/smoke/bin/pip install --upgrade pip
          /tmp/smoke/bin/pip install crates/sase_core_py/dist/*.whl
          /tmp/smoke/bin/python -c "import sase_core_rs; print(sase_core_rs.parse_query('status:Ready'))"

      - uses: actions/upload-artifact@v4
        with:
          name: wheels-macos-universal2
          path: crates/sase_core_py/dist

  windows:
    name: windows x86_64
    needs: release-plz-release
    runs-on: windows-latest
    if: ${{ github.repository_owner == 'sase-org' && ((needs.release-plz-release.outputs.releases_created == 'true' && !(github.event_name == 'workflow_dispatch' && inputs.dry_run)) || (github.event_name == 'workflow_dispatch' && inputs.build_wheels)) }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          architecture: x64

      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: release-windows-x64
          workspaces: ". -> target"

      - name: Build wheel
        uses: PyO3/maturin-action@v1
        with:
          target: x64
          working-directory: crates/sase_core_py
          args: --release --out dist --strip

      - name: Smoke test
        shell: pwsh
        run: |
          python -m venv $env:TEMP\smoke
          & $env:TEMP\smoke\Scripts\python.exe -m pip install --upgrade pip
          $wheel = Get-ChildItem crates\sase_core_py\dist\*.whl | Select-Object -First 1
          & $env:TEMP\smoke\Scripts\python.exe -m pip install $wheel.FullName
          & $env:TEMP\smoke\Scripts\python.exe -c "import sase_core_rs; print(sase_core_rs.parse_query('status:Ready'))"

      - uses: actions/upload-artifact@v4
        with:
          name: wheels-windows-x64
          path: crates/sase_core_py/dist

  sdist:
    name: sdist
    needs: release-plz-release
    runs-on: ubuntu-latest
    if: ${{ github.repository_owner == 'sase-org' && ((needs.release-plz-release.outputs.releases_created == 'true' && !(github.event_name == 'workflow_dispatch' && inputs.dry_run)) || (github.event_name == 'workflow_dispatch' && inputs.build_wheels)) }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build sdist
        uses: PyO3/maturin-action@v1
        with:
          working-directory: crates/sase_core_py
          command: sdist
          args: --out dist

      - uses: actions/upload-artifact@v4
        with:
          name: sdist
          path: crates/sase_core_py/dist

  metadata-check:
    name: twine check
    needs: [release-plz-release, linux, macos, windows, sdist]
    runs-on: ubuntu-latest
    if: ${{ github.repository_owner == 'sase-org' && ((needs.release-plz-release.outputs.releases_created == 'true' && !(github.event_name == 'workflow_dispatch' && inputs.dry_run)) || (github.event_name == 'workflow_dispatch' && inputs.build_wheels)) }}
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: pip install --quiet twine
      - run: ls -la dist
      - run: twine check dist/*

  publish:
    name: publish to PyPI
    needs: [release-plz-release, metadata-check]
    runs-on: ubuntu-latest
    if: ${{ github.repository_owner == 'sase-org' && ((needs.release-plz-release.outputs.releases_created == 'true' && !(github.event_name == 'workflow_dispatch' && inputs.dry_run)) || (github.event_name == 'workflow_dispatch' && inputs.build_wheels == true && inputs.publish_pypi == true && inputs.dry_run == false)) }}
    environment: pypi
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout repository for manual guard
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.publish_pypi == true }}
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Guard manual PyPI publish
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.publish_pypi == true }}
        env:
          EXPECTED_VERSION: ${{ inputs.expected_version }}
        run: |
          set -euo pipefail

          if [[ "${GITHUB_REF}" != "refs/heads/master" ]]; then
            echo "Manual PyPI publish must run from refs/heads/master; got ${GITHUB_REF}" >&2
            exit 1
          fi

          if [[ -z "${EXPECTED_VERSION}" ]]; then
            echo "expected_version is required for manual PyPI publish" >&2
            exit 1
          fi

          git fetch --force --tags origin
          head_commit="$(git rev-parse HEAD)"
          tag_commit="$(git rev-parse "v${EXPECTED_VERSION}^{commit}")"
          if [[ "${head_commit}" != "${tag_commit}" ]]; then
            echo "HEAD ${head_commit} does not match v${EXPECTED_VERSION} ${tag_commit}" >&2
            exit 1
          fi

          python - <<'PY'
          import email.parser
          import os
          import pathlib
          import tarfile
          import urllib.error
          import urllib.request
          import zipfile

          expected_version = os.environ["EXPECTED_VERSION"]
          expected_name = "sase-core-rs"
          dist = pathlib.Path("dist")
          files = [path for path in sorted(dist.iterdir()) if path.is_file()]
          if not files:
              raise SystemExit("dist/ contains no files")

          def read_metadata(path: pathlib.Path) -> str:
              if path.suffix == ".whl":
                  with zipfile.ZipFile(path) as archive:
                      metadata_names = [
                          name
                          for name in archive.namelist()
                          if name.endswith(".dist-info/METADATA")
                      ]
                      if len(metadata_names) != 1:
                          raise SystemExit(
                              f"{path}: expected one wheel METADATA file, found {len(metadata_names)}"
                          )
                      return archive.read(metadata_names[0]).decode("utf-8")

              if path.name.endswith(".tar.gz"):
                  with tarfile.open(path, "r:gz") as archive:
                      metadata_members = [
                          member
                          for member in archive.getmembers()
                          if member.isfile() and member.name.endswith("PKG-INFO")
                      ]
                      if len(metadata_members) != 1:
                          raise SystemExit(
                              f"{path}: expected one sdist PKG-INFO file, found {len(metadata_members)}"
                          )
                      extracted = archive.extractfile(metadata_members[0])
                      if extracted is None:
                          raise SystemExit(f"{path}: could not read sdist PKG-INFO")
                      return extracted.read().decode("utf-8")

              raise SystemExit(f"{path}: unsupported distribution file")

          for path in files:
              metadata = email.parser.Parser().parsestr(read_metadata(path))
              name = metadata.get("Name")
              version = metadata.get("Version")
              if name != expected_name or version != expected_version:
                  raise SystemExit(
                      f"{path}: expected Name={expected_name!r} Version={expected_version!r}, "
                      f"got Name={name!r} Version={version!r}"
                  )
              print(f"verified {path}: {name} {version}")

          url = f"https://pypi.org/pypi/{expected_name}/{expected_version}/json"
          request = urllib.request.Request(url, headers={"Accept": "application/json"})
          try:
              with urllib.request.urlopen(request, timeout=15):
                  raise SystemExit(f"{expected_name}=={expected_version} already exists on PyPI")
          except urllib.error.HTTPError as error:
              if error.code != 404:
                  raise
              print(f"verified {expected_name}=={expected_version} is absent from PyPI")
          PY

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
