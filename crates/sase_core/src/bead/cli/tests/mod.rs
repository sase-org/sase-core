//! Bead CLI tests, split along the same seams as the production
//! modules so each test file sits beside the code it covers.

mod create_command;
mod design_refs;
mod mutate_commands;
mod parsing;
mod read_commands;
mod rendering;
mod support;
