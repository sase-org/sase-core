//! Completion tests, split along the same seams as the production
//! modules so each test file sits next to the code it covers.

mod artifact_ref;
mod assist_candidates;
mod directive_candidates;
mod support;
mod trigger_context;
mod vcs_candidates;
